{
	"name": "Debian",
	"dockerComposeFile": "../docker-compose.yml",
	"service": "devcontainer",
    "workspaceFolder": "/workspace",
	"onCreateCommand": "apt-get update && apt-get install -y python3-mysqldb mariadb-client git --no-install-recommends",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-azuretools.vscode-docker",
				"ms-python.python"
			]
		}
	}
}
